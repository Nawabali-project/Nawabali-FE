import styled from 'styled-components';

const Main = () => {
  return (
    <Layout>
      <h1>메인 페이지~</h1>
    </Layout>
  );
};

const Layout = styled.div`
  padding-top: 61.25px;
`;

export default Main;
